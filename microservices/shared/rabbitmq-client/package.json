{
  "name": "@open-erp/rabbitmq-client",
  "version": "1.0.0",
  "description": "Shared RabbitMQ client library with retry, backoff, and idempotency handling",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "amqplib": "^0.10.3",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.4",
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3"
  }
}
