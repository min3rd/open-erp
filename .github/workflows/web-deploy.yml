name: Deploy Web Frontend to Dev Cloud

on:
  push:
    branches:
      - develop
    paths:
      - 'open-erp-web/**'
      - '.github/workflows/web-deploy.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: open-erp-web

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: open-erp-web/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Run Linting
        run: npm run lint

      - name: Run Build (Verify Angular compiler)
        run: npm run build -- --configuration=production

      - name: Log in to Docker Hub/Registry
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./open-erp-web
          push: true
          tags: myregistry/open-erp-web:${{ github.sha }}

      - name: Configure Cloud Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-southeast-1

      - name: Deploy to Kubernetes Cluster (EKS)
        run: |
          aws eks update-kubeconfig --name open-erp-dev-cluster
          kubectl set image deployment/web-frontend-deploy web-frontend=myregistry/open-erp-web:${{ github.sha }}
          kubectl rollout status deployment/web-frontend-deploy
