apiVersion: v1
kind: ConfigMap
metadata:
  name: api-gateway-config
  namespace: open-erp
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/part-of: open-erp
data:
  NODE_ENV: staging
  PORT: "3000"
